# Every script npm runs here resolves @aweftjs/* to its TypeScript source rather than to the
# compiled dist/ a published package carries (design 256). An application that holds this repo
# as a submodule writes the same line in its own .npmrc.
node-options=--conditions=aweft-source
