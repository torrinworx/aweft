#!/bin/sh
# The gate and the audit, before anything leaves this machine. `npm install` at the root points
# core.hooksPath here through the prepare script.
set -e
cd "$(git rev-parse --show-toplevel)"
npm test
npm audit --audit-level=high
