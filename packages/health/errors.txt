invalid-config: Give checks a function per name, each answering what to report under that name.
invalid-config: Give info an object JSON can write, such as { build: "abc123" }.
invalid-config: Set public to true for a poll anyone may make, or false for one that needs a signed-in user.
